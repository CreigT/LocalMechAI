[project]
name = "localmechai"
version = "0.1.0"
description = "Local-first Windows system health and diagnostics agent."
requires-python = ">=3.10"
dependencies = [
  "psutil>=5.9.8",
  "requests>=2.32.0",
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "pydantic>=2.8.0"
]

[project.optional-dependencies]
adk = [
  "google-adk>=1.0.0"
]

[tool.ruff]
line-length = 100
